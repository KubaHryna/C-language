#include <stdio.h>
#include <stdlib.h>
int min(int* x,int* y){
    if(*x>*y)
        {
        return *y;
    }
    else
    {
    return *x;
    }
}

int main(){
int a=7;
int b=9;
printf("%d",min(&a,&b));

return 0;
}
